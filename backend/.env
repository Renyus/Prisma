# ==============================================================================
# 🌸 Prisma 配置文件
# ==============================================================================
# 数据库 (默认 SQLite)
DATABASE_URL="sqlite:///./sakura.db"

# ======================== 1. 全局基底配置 (Global) ========================
# 如果下方的模块没有单独指定 Key/URL，将默认使用这里的值。
# 推荐填入 SiliconFlow 或 OpenRouter 等聚合平台的 Key。

GLOBAL_LLM_KEY=""
GLOBAL_LLM_URL="https://api.siliconflow.cn/v1"

# GLOBAL_LLM_URL="https://api.siliconflow.cn/v1"


# ======================== 2. 主对话模块 (Chat) ========================
# 负责与用户进行交互，建议使用高智商模型。

CHAT_MODEL="deepseek-ai/DeepSeek-V3.2"

# [可选] 独立配置：如果您希望对话使用不同的供应商 (如 OpenAI)，请在此填入。
# 留空则自动继承 GLOBAL_LLM_KEY 和 GLOBAL_LLM_URL。
CHAT_API_KEY=""
CHAT_API_URL=""


# ======================== 3. 工具模块 (Summary & Memory) ========================
# 负责后台总结历史、提取记忆。建议使用便宜、速度快、指令遵循好的模型。

UTILITY_MODEL="Pro/Qwen/Qwen2-7B-Instruct"

# [可选] 独立配置：如果您希望工具模型运行在本地 (如 Ollama) 或其他便宜的节点。
# 留空则自动继承 GLOBAL_LLM_KEY 和 GLOBAL_LLM_URL。
UTILITY_API_KEY=""
UTILITY_API_URL=""

# 自动总结触发阈值 (条数)
SUMMARY_HISTORY_THRESHOLD=15


# ======================== 4. 向量记忆模块 (RAG) ========================
# 负责长期记忆的存取。需要 Embedding 模型支持。

RAG_EMBEDDING_MODEL="Pro/BAAI/bge-m3"

# [可选] 独立配置
# 留空则自动继承 GLOBAL_LLM_KEY 和 GLOBAL_LLM_URL。
RAG_API_KEY=""
RAG_API_URL=""

# 向量数据库路径
RAG_VECTOR_DB_PATH="./chroma_db"